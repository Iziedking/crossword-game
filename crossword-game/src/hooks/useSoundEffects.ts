export function useSoundEffects() {
  const playCorrectWord = () => {
    // Sound effect placeholder
  }

  const playLevelComplete = () => {
    // Sound effect placeholder
  }

  return {
    playCorrectWord,
    playLevelComplete,
  }
}
